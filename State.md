# State — Rodina Moskva

- **2026-02-20**: Вынесены стили в `style.css`, скрипты в `main.js`. Добавлен State.md. HTML подключает внешние файлы.
- **2026-02-20**: Бэкенд регистрации: `server/` — Express, SQLite, POST `/api/register`. Форма в main.js шлёт заявки на API. Запуск: `cd server && npm install && npm start` (порт 3001).
- **2026-02-25**: Создана папка `c:\Projects\server`: Express, CORS, sql.js (SQLite без нативной сборки), БД `rodina.db`. Запуск: `cd c:\Projects\server`, `npm install`, `npm start`.
